# Python
__pycache__/
*.pyc
agent/.venv/

# Build output (regenerated by agent/build-exe.bat)
agent/build/
agent/dist/
agent/*.spec

# App-level Google OAuth client — each distributor bundles their own.
# See agent/resolve_sync/branding.py for the format; the app runs fine
# without it (Google sign-in just shows as not configured).
agent/resolve_sync/google_client.json

# Local development context (machine-specific notes)
CLAUDE.md
